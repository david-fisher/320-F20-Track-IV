/* eslint-env mocha */
const db = require("../db");
const assert = require("assert");

describe("conversation_task DB access points test", function () {
  before(function () {});
  after(function () {});
  describe("test getConversationTask", function () {
    before(function () {});
    after(function () {});
    beforeEach(function () {});
    afterEach(function () {});
    it("Should return a valid conversation task page with valid pageID", function (done) {});
    it("Should return null with invalid pageID", function (done) {});
  });

  describe("test createConversationTask", function () {
    before(function () {});
    after(function () {});
    beforeEach(function () {});
    afterEach(function () {});
    it("Should create a valid conversation task page with valid pageID and valid content", function (done) {});
    it("Should throw error with invalid pageID", function (done) {});
    it("Should throw error with invalid content", function (done) {});
  });

  describe("test updateConversationTask", function () {
    before(function () {});
    after(function () {});
    beforeEach(function () {});
    afterEach(function () {});
    it("Should update a valid conversation task page with valid pageID and valid content", function (done) {});
    it("Should return null with invalid pageID", function (done) {});
    it("Should throw error with invalid content", function (done) {});
  });

  describe("test deleteConversationTask", function () {
    before(function () {});
    after(function () {});
    beforeEach(function () {});
    afterEach(function () {});
    it("Should delete a valid conversation task page with valid pageID", function (done) {});
    it("Should return null with invalid pageID", function (done) {});
  });
});
